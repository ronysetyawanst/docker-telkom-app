language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t ronysetyawanst/docker-telkom-app -f Dockerfile.dev .

script:
  - docker run -e CI=true ronysetyawanst/docker-telkom-app npm run test